# This is a Databricks asset bundle definition for gh_demo.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: gh_demo

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3352645888015995.15.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-3352645888015995.15.azuredatabricks.net
      # We explicitly specify /Users/kevin.romero@databricks.com to make sure we only have a single copy.
      root_path: /Users/kevin.romero@databricks.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: kevin.romero@databricks.com
        level: CAN_MANAGE
    run_as:
      user_name: kevin.romero@databricks.com
